# FRIDAY — MASTER PLAN & PROGRESS

> This file is the single source of truth. Claude Code MUST update it after
> every task: tick the checkbox, write what was done, and note what's next.
> On a new session or after running out of context, READ THIS FILE FIRST and
> resume from the first unchecked task. Never redo finished work.

---

## REPO & GIT RULES (do this on every task)
- Remote: `https://github.com/saurabh-v12/Friday-extension.git`
- First task only: `git init`, add remote, first commit + push (`-u origin master`).
- After EVERY task below: `git add -A` → `git commit -m "<task msg>"` → `git push`.
- Small, frequent commits — one per task, never batch.
- After each task: update THIS file (check the box + status), then commit it too.
- `git config user.email` must match the GitHub account or contributions won't count.

---

## WHAT WE ARE BUILDING (one line)
A Chrome extension "Friday" that reads the screen with a local in-browser AI,
redacts private data on-device, and acts on the page by voice or text — with
nothing sensitive ever leaving the machine.

## LOCKED DESIGN DECISIONS (do not change without updating this file)
- **DOM for control, vision for understanding.** Clicks/typing target real DOM +
  accessibility-tree elements (near-100% accuracy). Vision is only for screen
  understanding + PII detection.
- **On-device core = DOM + a11y + BlazeFace + Tesseract.js.** Millisecond-scale,
  no local VLM on the critical path. (Gate 0.7 confirmed the in-browser VLM is
  functionally correct but ~100× too slow on typical Intel-iGPU/CPU hardware
  for live use.)
- **Reasoning / screen-understanding = BYOK cloud** (Gemini Flash / OpenAI /
  Groq) via the **Brain Router**. Redaction ALWAYS runs on-device BEFORE any
  API call — the privacy boundary sits before the wire, not after.
- **Local in-browser VLM = OPTIONAL, off-by-default fallback** for strong-GPU
  users. Stays in the repo (popup Test AI + Run on sample) but is not on the
  primary path. No further performance chasing.
- **Fully in-browser, zero user setup** for the on-device core: DOM/a11y is
  free, detectors download once and cache. BYOK requires the user's own key,
  stored in `chrome.storage.local`.
- **Privacy boundary is absolute:** redaction runs on-device BEFORE any network
  or tool call.
- Chrome first. One flawless demo over many half-working features.

## STACK
Manifest V3 · JavaScript/TypeScript · Transformers.js + WebGPU · BlazeFace ·
Tesseract.js · Canvas API · content script (DOM control) · Web Speech API ·
BYOK (Gemini/OpenAI/Groq) · MCP client (stretch) · chrome.storage.local

## UNIQUE FEATURES (beyond the problem statement)
1. Voice-first agentic control ("Hey Friday").
2. Privacy receipt — shows exactly what was redacted, live.
3. Brain Router — local for speed/privacy, BYOK for hard tasks.
4. MCP connections — act across apps, PII redacted first.
5. Learns private items — mark once, always redacted.

---

## UI DESIGN SPEC (LOCKED — build the side panel to match this)
Reference mockup lives at `design/friday-ui.png` in the repo.

**Theme:** light — white / very-light (#f7f7f8) background, black text, crimson
accent `#ff2e63`. Rounded corners, generous whitespace, clean sans-serif.
Tall vertical sidebar (extension side panel / popup).

**HEADER (one row, thin bottom border):**
- Left: red Friday wing-logo + "Friday" in bold black.
- Center: a dropdown pill labelled "Chat / Agent" (switches mode).
- Right: a toggle switch showing cloud icon <-> monitor icon (Cloud vs On-Device
  mode), then a settings gear icon.

**BODY (empty state, centered vertically):**
- Black cat logo image (`black cat.png`).
- Heading: "Hey! I'm " in black + "Friday" in crimson, bold, large.
- Subtitle (grey, two lines): "Your private AI agent, running on your device." /
  "No data leaves your browser."
- (Later phases add command chips + the privacy-receipt card here.)

**INPUT BAR (bottom, rounded box, light border):**
- Placeholder: "Hey!, Wassup what you doing now."
- Bottom row inside the box: "+" button (left), a "Select Model" dropdown, a mic
  icon, and a black circular send button (right).

**Notes:** keep it minimal, match spacing/layout of the mockup. Privacy status
("On-device / 0 data sent") is shown via the header Cloud/On-Device toggle.

## POST-DEMO (deferred work — do NOT start until after SIH demo ships)
- **WebLLM local reasoning** — implement on `feat/webllm-local` branch after
  SIH demo. Use the WebLLM plan and safety rules from the prior message.

## PROGRESS LOG (newest at top — Claude Code appends here each session)
- 2026-09-11 - fix(gpu): request the high-performance WebGPU adapter for local models. User saw `Finish loading on WebGPU - intel (100%)` even though the laptop has an RTX 4050; root cause is that Friday's own WebGPU probes used `navigator.gpu.requestAdapter()` with no preference, so Chrome/Windows could report the integrated Intel adapter. `src/localLlm.js#detectLocalLlmSupport()` and `src/model.js#detectWebGPU()` now call `navigator.gpu.requestAdapter({ powerPreference: "high-performance" })`. WebLLM's vendored runtime already defaults its internal GPU detection to high-performance, so this primarily aligns Friday's status checks and VLM probe with the desired dGPU path. Caveat for demo: browser code can only request high-performance; Windows/NVIDIA still decide final routing, so set Chrome to High performance in Windows Graphics settings if the label remains Intel after reload. Verified with `node --check src/localLlm.js`, `node --check src/model.js`, and `npm run build`.
- 2026-09-11 - fix(local-agent): restored local action execution after the visual-screen experiment was undone. User confirmed Cloud works but On-Device Qwen in Chat mode replied `I cannot proceed from the current page` for commands like `play/click the second video`. Current state keeps the user's rollback of the Local VLM visual-screen path, preserves deterministic `open youtube` navigation in `src/shortcuts.js`, and restores the generic local ordinal action fallback in `src/localAgent.js`: `play/open/watch/start/click/press/select the first/second/third/fourth/fifth video/button/link/field/heading/item` now executes `EXEC_TOOL clickOrdinal({kind,index})` directly before asking the small local model. `src/sidepanel-main.js` promotes local Chat-mode page-action requests into the local tool flow with Agent semantics, so the default Chat pill can still act when the command is clearly an action. Verified with `node --check src/localAgent.js`, `node --check src/sidepanel-main.js`, `node --check src/pageAnswers.js`, `npm run build`, and `git diff --check`. Manual browser check: reload extension, choose On-Device + Qwen, stay in Chat mode, run `open youtube`, then `play the second video`; a `clickOrdinal video #2` chip should appear and the video should open.
- 2026-09-11 - feat(voice): added best-effort headset button activation. `src/sidepanel-main.js` now treats physical headset/media-button taps as a mic click when the Friday side panel is open: listens for `KeyboardEvent` `MediaPlayPause`, registers Media Session handlers for `play`, `pause`, `stop`, and `togglemicrophone`, debounces duplicate hardware events, starts/stops the same one-shot dictation path as the visible mic button, and shows `Headset button detected. Listening...`. Limitation documented for demo: headset media buttons are OS/browser/device dependent and may go to the currently active media page/app unless Chrome routes them to Friday. Verified with `node --check src/sidepanel-main.js` and `npm run build`. Manual browser check: reload extension, open Friday side panel, tap the headset mic/media button, confirm the voice status changes and dictation starts.
- 2026-09-11 - fix(voice): expanded wake-word recognition aliases after user testing showed Chrome sometimes misses/transcribes `Friday` poorly. `src/voice.js#startWakeWord()` now defaults to multiple common recognitions (`friday`, `hey friday`, `fri day`, `free day`, `fry day`, `freddy`, `hey freddy`), and `src/sidepanel-main.js` passes the same aliases into auto wake mode. Researched LiveKit as an alternative voice path: it can be used, but it is not a drop-in wake-word fix because LiveKit is a realtime audio/agent transport that needs a LiveKit room token and usually a local/cloud agent server with STT. For the immediate demo, this commit keeps the no-server Chrome extension path and improves wake matching. Verified with `node --check src/voice.js`, `node --check src/sidepanel-main.js`, and `npm run build`. Manual browser check: reload extension and try `free day play the second video`, `fry day what is on my screen`, and `freddy summarize this page`.
- 2026-09-11 - fix(voice): made wake mode stricter after user testing showed confusing partial activations. `src/voice.js#startWakeWord()` now sorts wake phrases longest-first, strips repeated leading wake phrases from the captured command, ignores wake-only speech like `friday friday hey friday` instead of submitting it as chat, and exposes `onIdle` so the UI can reset after timeout/ignored wake text. `src/sidepanel-main.js` removes the spoken `Yes?` wake acknowledgement so Friday does not hear its own TTS as the next command, stops wake recognition before running a wake-submitted command, resumes wake only after the agent finishes, and delays manual mic wake resume until the submitted task completes. Verified with `node --check src/voice.js`, `node --check src/sidepanel-main.js`, manifest JSON parse, and `npm run build`. Manual browser check: reload extension, say only `Friday Friday hey Friday` and confirm nothing submits; then say `Friday play the second video` and confirm the status returns to `Wake word active` after the response.
- 2026-09-11 - fix(voice): hardened wake-word listening for live demos. Fixed the one-shot dictation error path in `src/voice.js`, kept pending wake commands from being discarded when Chrome auto-ends SpeechRecognition, separated interim and final wake transcripts so commands don't duplicate words, accepts both `Friday ...` and `hey Friday ...`, waits longer after the wake word for natural pauses, and restarts wake mode after a spoken command submits. `src/sidepanel-main.js` now pauses wake listening before manual mic dictation so two recognizers do not fight each other, treats `no-speech`/`aborted` wake events as normal restarts instead of scary errors, and keeps the voice status line on `Wake word active. Say "Friday".` when the recognizer restarts. Verified with `node --check src/voice.js`, `node --check src/sidepanel-main.js`, manifest JSON parse, and `npm run build`. Manual browser check: reload the extension, keep the side panel open, allow mic, say `Friday` then within 8 seconds say a task, or say `Friday play the second video` in one sentence.
- 2026-09-11 - feat(voice): wake word now auto-arms when the side panel opens and listens for just `Friday`. `src/voice.js#startWakeWord()` now defaults to `phrase="friday"`, can handle one-shot utterances like `Friday play the second video` without needing a second speech chunk, and stops restart loops on permission/audio-capture failures. `src/sidepanel-main.js` starts wake listening automatically after boot, preflights microphone permission for wake mode, shows `Wake word active. Say "Friday" to start.`, pauses wake recognition while one-shot dictation runs, then resumes wake mode after dictation submits or ends. Long-press/right-click still toggles wake listening manually. Verified with `node --check src/voice.js src/sidepanel-main.js`, manifest JSON parse, `npm run build`, and `git diff --check`. Manual browser check: reload the extension, allow mic if prompted, say `Friday play the second video`; Friday should wake and submit the command.
- 2026-09-11 - fix(voice): made the mic button reliable for speak-instead-of-type demos. `manifest.json` now requests `audioCapture`; `src/voice.js` adds a microphone permission preflight and stops the test stream immediately, then `startDictation()` delivers final text only once. `sidepanel.html` adds a small live `#voiceStatus` line and a clearer mic tooltip. `src/sidepanel-main.js` now shows visible voice states (`Listening`, `Heard: ...`, `Submitting voice command`, permission/no-speech errors), handles blocked mic permission clearly, aborts an active dictation on second click, and uses pointer/click events so normal click works while long-press/right-click still toggles wake word. Verified with manifest JSON parse, `node --check src/voice.js src/sidepanel-main.js`, `npm run build`, and `git diff --check`. Manual browser check: reload the extension, click mic, allow microphone, speak a command, and confirm it auto-submits.
- 2026-09-11 - feat(branding): switched Friday's icons to the provided cat artwork. `manifest.json` now uses `white cat.png` for the extension/app/action icon outside the side panel, and `sidepanel.html` uses `black cat.png` for both the header brand mark and empty-state logo instead of the old crimson spark/plus-like SVG. `sidepanel.css` gives the header icon a stable 26px circular crop and the home logo a 76px contained image box so the cats do not stretch or shift layout. Verified with manifest JSON parse, `npm run build`, and `git diff --check`.
- 2026-09-11 - feat(local-llm): added WebLLM as Friday's fast local text reasoning path so Cloud token/rate-limit failures can fall back to the laptop. New dependency `@mlc-ai/web-llm@0.2.85` is vendored by `scripts/build.mjs` into `dist/vendor/webllm` for MV3-safe local imports. New `src/localLlm.js` loads and caches a WebGPU model, defaulting to `Qwen2.5-1.5B-Instruct-q4f16_1-MLC` for the user's RTX 4050 6GB / i7 HX laptop, with faster 0.5B/1B and stronger 3B/Phi options in Settings. New `src/localAgent.js` implements an offline JSON planning loop over the existing generic DOM tools (`click`, `type`, `scroll`, `goto`, `clickOrdinal`, `readText`, `getSnapshot`), so arbitrary page tasks do not depend on canned website commands. `src/sidepanel-main.js` now routes On-Device Chat to WebLLM plain chat, On-Device Agent to the local tool loop, and Cloud 429/quota/no-key failures into the same local fallback while flipping the header back to On-Device. Settings now includes a "Local LLM (offline)" row with enable, model select, load button, and progress; the composer model pill reflects the selected local/cloud model. Verified with `npm run build`, `node --check` on changed modules, `npm ls --depth=0`, `git diff --check`, and a model-id smoke import. Manual browser check still needed: reload the unpacked extension, open Settings, load the local LLM once, switch to On-Device + Agent, then ask `play the 2nd video on my screen`.
- 2026-09-11 - fix(agent-harness): replaced the YouTube-specific "play nth video" shortcut direction with a generic AI-controlled ordinal action harness. User correctly called out that judge demos must show Friday understanding arbitrary websites and tasks, not canned commands. `src/shortcuts.js` no longer intercepts `play/open/watch/click the 2nd video`; those phrases now route to the Cloud tool-calling agent. `content.js#collectCompactSnapshot()` now emits screen-ordered visible items with `screenIndex`, `kind` (`video`, `button`, `link`, `field`, `heading`, `item`), stable `selector`, and `bbox`, so the model sees a screen-like inventory instead of noisy DOM order. New generic `EXEC_TOOL clickOrdinal({kind,index})` clicks/focuses the Nth visible item of a kind, sorted top-to-bottom then left-to-right; YouTube video detection is now only an internal candidate classifier for `kind:"video"`, not a phrase shortcut. `src/chatAgent.js` exposes `clickOrdinal` as a general tool and instructs the model to use it for ordinal requests like "second video", "first button", or "third link"; compacted snapshot tool results preserve kind/index/bbox while staying token-safe. `src/sidepanel-main.js` renders generic `clickOrdinal` trace chips. Verified with `npm run build`, JS syntax checks, `git diff --check`, and a Node smoke test confirming video phrases no longer match shortcuts while trivial commands like `scroll down` still bypass the LLM.
- 2026-09-11 - fix(youtube/control): "play the 2nd video on my screen" now bypasses the LLM and clicks the visible YouTube video directly. User screenshot showed Groq first 429ing, then the tool loop choosing `scroll`/`getSnapshot` and exploding to `Prompt is 7842 tokens`; root cause was that ordinal video commands were left to the model instead of using deterministic DOM control, and `getSnapshot` tool results were fed back with all 150 elements. `src/shortcuts.js` now matches `play/open/watch/click the 2nd/second/... video (on my screen/page/youtube/tab)` before agent routing and calls `EXEC_TOOL clickNthVideo` with a 1-based index. `content.js` implements `clickNthVideo`: finds visible YouTube `/watch?v=` anchors, dedupes by video href, sorts visible cards top-to-bottom then left-to-right, clicks the requested card, and returns title/count for the chat bubble. `src/chatAgent.js` also exposes `clickNthVideo` as a fallback tool, and now compacts large tool results: `readText` is capped at 2000 chars and `getSnapshot` results are reduced to safe URL/title, 500 chars of visible text, element count, and the first 24 lean elements before they are appended to the next model call. `src/sidepanel-main.js` renders `clickNthVideo` trace chips nicely. Verified with `npm run build`, JS syntax checks, `git diff --check`, and a Node shortcut smoke test covering the user's exact phrase.
- 2026-09-11 - fix(ui/page-answers): New chat is now discoverable in long conversations, and page Q&A catches the user's actual phrases. User screenshot showed the run header had scrolled away, hiding `#newChatBtn`; `sidepanel.css` now makes `.run-head` sticky at the top of the chat view, and `sidepanel.html` changes the icon-only button into a compact `+ New` pill. The always-visible composer `#plusBtn` is now labelled `New chat` and `src/sidepanel-main.js#wireComposer()` wires it to the existing `newChat()` handler, so the visible `+` button clears history and returns home. Page-answer routing expanded in `src/pageAnswers.js`: `can you see my screen/page` now matches the local two-line screen description path, and general page questions like `what's the main heading on this page?`, `what buttons are on this page?`, and `which links are on this webpage?` use a new `page-question` intent. Local heuristics answer main-heading/buttons/links without an API call; other page questions use BYOK with scrubbed visible text when configured, or fall back to the local extractive summary. `content.js#COMPACT_SELECTOR` now includes `h1-h6` and `[role=heading]`, so main-heading answers can inspect real heading nodes instead of guessing from body text. Verified with `npm run build`, `git diff --check`, and Node smoke tests covering the screenshot phrases.
- 2026-09-11 - fix(page-answers): screen/page questions no longer fall through into the tool-calling prompt cap. User hit `Prompt is 1207 tokens (over 1200 cap)` while trying to inspect the page; root cause was twofold: `src/pageAnswers.js#SCREEN_RE` only matched exact phrasings, so natural variants like `what can you see?` could route to `chatAgent`, and `src/chatAgent.js` passed all saved `chatHistory` into the tool-calling request untouched. Fix: broadened screen-description intent matching to include `what can/do you see`, `tell me what's on the screen/page`, and `read the screen/page/webpage`; tightened `chatAgent` prompt shape from 30 to 24 elements and 40 to 32 chars per field; added `safePageUrl()` so long/query-heavy URLs don't waste tokens or leak query params into the prompt; added `compactHistory()` to keep only recent user/assistant text, truncate each saved message to 300 chars, and drop oldest messages until the initial request is under the existing 1200-token cap. This preserves the rate-limit protection while avoiding the false failure at ~1207 tokens. Verified with `npm run build`, `git diff --check`, and a Node smoke test covering `what can you see?`, `what do you see on this page?`, `tell me what is on the screen`, `read the webpage`, summary intents, and non-page commands like `scroll down`/`click submit`.
- 2026-09-11 - feat(page-answers): Chat can now answer "what's on the screen?" and summarize the current webpage. New `src/pageAnswers.js` detects page-answer intents before generic chat: screen-description requests (`what's on the screen`, `describe this page`, `what am I seeing`) use `SNAPSHOT_COMPACT` and return exactly two short lines from the local DOM/screen snapshot, so this works even when Cloud is not configured and costs zero API calls. Summary requests (`summarize this webpage/article/page`, `tl;dr`, `recap`) read visible page text locally via `EXEC_TOOL:readText`, scrub obvious PII with the existing `PATTERNS` (`email`, Aadhaar, PAN, SSN, card, IN/US phone), then call the selected BYOK model only when Cloud + API key are configured; otherwise they fall back to a local extractive summary from the visible text. `src/sidepanel-main.js` now imports `matchPageAnswer/runPageAnswer` and routes matched tasks immediately after the existing shortcut bypass, before the old Chat-mode plain chat path that had no page context. `content.js#readText` now accepts bounded `maxChars` (500-10000, default 5000) so page summaries stay token-safe. Verified with `npm run build` and a Node smoke test for intent matching + PII scrubbing (`Email [email] phone [phone] PAN [pan]`). Manual browser check still recommended: reload the unpacked extension, open any normal http(s) page, ask `what's on the screen?` (should return two lines) and `summarize this webpage` (should summarize page text, cloud if BYOK is configured, local fallback otherwise).
- 2026-09-11 — feat(ui) tweak 2: header "Scan" pill beside the Cloud/On-Device toggle. `sidepanel.html` — inserted `<button class="pill-btn pill-btn--sm" id="headerScanBtn">` between `#deviceToggle` and `#settingsBtn`, with the shield SVG (same path as the receipt-badge) + short "Scan" label. `aria-label="Scan privacy"` + `title` tooltip explain the action for screen readers and hover. `sidepanel.css` — one new modifier `.pill-btn--sm { margin-top: 0; padding: 4px 8px; font-size: 11px; gap: 4px; }` that zeroes out the empty-state's `margin-top: 18px` and shrinks the pill for header use. `src/sidepanel-main.js` — `wireReceipt()` now also listens on `#headerScanBtn` and calls the existing `runScan()`. **No stub** — the privacy pipeline (`runPrivacyPipeline` → BlazeFace + Tesseract + DOM PII → redact → receipt card) already exists and works from any view, so user's rule 3 (stub when pipeline missing) doesn't apply. Reentry-safe: `runScan()`'s `scanInFlight` guard already blocks double-clicks; the header button doesn't get its own busy-state label (minimum-code choice — the empty-state button and receipt "Scan again" button already carry the status text). Existing "Run privacy scan" pill in the empty state kept — the header button is an addition, not a replacement.
- 2026-09-11 — feat(ui) tweak 1: close (X) button in Settings top-right. `sidepanel.html` — added `<button class="icon-btn settings-close" id="settingsCloseBtn">` inside `.settings-header` after the h2, reusing the same X SVG path as `#runCloseBtn`. `sidepanel.css` — one line: `.settings-header .settings-close { margin-left: auto; }` pushes it to the far right of the flex row. `src/sidepanel-main.js` — `wireSettings()` now also listens on `#settingsCloseBtn` and calls the existing `closeSettings()`. Escape key + back arrow still work. Per-row Save buttons (byok, mcp) untouched.
- 2026-09-11 — feat(byok) item 4: 429 backoff-and-retry on Groq/OpenAI tool calls. **User picked option B** for item 3 (skip cross-provider Gemini fallback — items 1+2 already dropped API pressure ~10× so a same-provider retry is enough for the demo). New `byok.js#parseRetryAfterMs(errMsg)` regex-extracts `try again in X.Xs / Xms / Xm` from the provider's error body — returns milliseconds or null. New `byok.js#chatWithToolsRetry(args, {onBackoff})` wraps `chatWithTools`: catches `err.status === 429`, waits `(parsedMs || 5000) + 500` ms, retries once. Non-429 errors (400 bad request, 401 bad key, network fails) propagate immediately — retrying those never helps. `chatAgent.js` now imports `chatWithToolsRetry` in place of `chatWithTools` and passes an `onBackoff` callback that emits a new `phase:'backoff'` event with `waitMs`/`parsedMs`. `sidepanel-main.js#runChatToolsFlow` handles the new phase by setting the status pill to `"Rate limited — retrying in Ns…"` instead of freezing on "Calling model". Backoff hook is wrapped in try/catch so a broken UI callback can't kill the retry. Console log `[backoff] status=429 waitMs=<n> parsedMs=<n>` for verification. Verified `parseRetryAfterMs` against 6 realistic Groq strings via Node smoke: `6.2s→6200`, `750ms→750`, `1m→60000`, `0.5s→500`, unknown format `→null`, "Retry in X" (wrong verb) `→null`. Only one retry attempt on purpose — if the second call also 429s, the friendly error bubble ("hit the limit again") surfaces immediately rather than looping. Item 3 skipped by user choice; item 4 alone plus items 1+2 should carry the demo. Verify: rerun "scroll down" (should hit `[shortcut] cmd=scroll`, no API call) and a real tool-calling task like "click submit" (should see `[ctx] promptTokens=` well under 800, and no 429 in normal use — but if it does 429, the pill goes yellow with "Rate limited — retrying in Xs…" instead of erroring).
- 2026-09-11 — feat(shortcuts) item 2: intent shortcuts bypass the LLM entirely for six trivial commands — `scroll (up|down|top|bottom)`, `reload|refresh (page|tab)?`, `(go )?back`, `(go )?forward`, `(open )?new tab`, `close (this )?tab`. New module `src/shortcuts.js`: `matchShortcut(task)` returns `{name, match, handler}|null`, `runShortcut()` logs `[shortcut] cmd=<name>` and executes. **Zero API cost** for the scroll demo path — the composer submit intercepts and returns before touching `pickSubmitFlow`. **Wiring**: `src/sidepanel-main.js#onSubmitComposer` checks `matchShortcut()` right after the empty-task guard; on hit it opens the run view, appends the user bubble, executes, appends the result bubble, and returns — no snapshot, no chatAgent, no chatWithTools call. Reuses the same `agentInFlight` + `setComposerBusy` guards as the LLM path so double-submits are impossible. **Permissions**: `scroll` reuses the existing EXEC_TOOL path (already exposed via content.js's `execTool`). `reload/close-tab/new-tab` use `chrome.tabs.reload/remove/create` from the sidepanel — no `tabs` permission needed since we already have `activeTab`. `back/forward` use `chrome.scripting.executeScript({func: () => history.back()})` in the active tab — deliberate choice over `chrome.tabs.goBack/goForward` which require the heavier `tabs` permission (out of scope for a demo fix). Manifest untouched. **Regex hygiene**: patterns are anchored `^…$`, allow trailing punctuation and optional filler words ("go", "this", "open a"), reject bare "scroll" (needs a direction). Ran a 12-case Node smoke test — all 9 positive cases match the right shortcut, all 3 ambiguous ones (bare "scroll", "hey scroll down please", "submit the form") correctly return null. Verify per spec: sidepanel console shows `[shortcut] cmd=scroll` on the next "scroll down" run and NO `[ctx] promptTokens=` log (because chatAgent never runs).
- 2026-09-11 — perf(chatAgent) item 1: slash prompt context to fit Groq free-tier rate limits. Root cause of 429s on every request: `"scroll down"` first turn was ~4200 prompt tokens — `systemPrompt()` was sending url+title+80 elements+**3000 chars of visibleText**, and the visibleText alone was 750+ tokens on a content-heavy page. **Fix** (`src/chatAgent.js`): drop `VISIBLE TEXT` section from the system prompt entirely (`readText` tool still works when the model needs it), reduce element cap 80 → **30** (`MAX_PROMPT_ELEMENTS`), tighten per-field truncation to **40 chars** for name/text/placeholder/href (`MAX_FIELD_CHARS`) — was 60/60/40/80. New `estimatePromptTokens(messages)` sums content + tool_calls + tool_call_id at ~4 chars/token; logged as `[ctx] promptTokens=<n>` on every outbound call (via `console.log` so it lands in the sidepanel DevTools console). Hard-fail if > 1200: throw a labeled error + emit `phase:'error'` so the UI shows "Prompt is N tokens (over 1200 cap). Start a new chat or refresh the tab." — better than silently hitting a 429 on the model side. Ballpark for "scroll down" first turn on a typical page: **~500–700 tokens** now (system boilerplate + 30 elements × ~15 tokens + user msg). Content-side `collectCompactSnapshot()` still returns up to 150 elements — deliberately unchanged, since chatAgent is now the client-side gate and other paths (future) might want the fuller list. Emitted `promptTokens` bubbled up into `phase:'model-call'` so the UI could show it later (not wired yet — out of scope). Verify per spec: check the sidepanel console for `[ctx] promptTokens=` on the next real run.
- 2026-09-11 — test(demo) item 1: agent scroll end-to-end verified on a real page. `[demo] test=scroll ok=true`. User ran "scroll down" in the Friday panel against a live tab with Cloud/Groq/`openai/gpt-oss-20b` selected — page scrolled as expected. No code change needed; commit message adjusted from the spec's `fix(agent):` to `test(demo):` since nothing was broken. **Observation to address before demo:** hitting Groq free-tier rate limit sooner than expected — needs investigation before item 2 (likely candidates: chatAgent tool loop making 2–3 calls per turn, retained chat history growing prompt tokens, or the 150-element snapshot payload. NOT touching yet — waiting on user direction, staying inside "no fix-forward" rule).
- 2026-09-11 — chore(plan): add POST-DEMO section; defer WebLLM local reasoning to `feat/webllm-local` (branch not cut yet — nothing to abandon since I checked in before creating it). Rest of today locked to demo scope: BYOK Groq (`openai/gpt-oss-20b`) as brain, DOM + a11y for control, BlazeFace + Tesseract + DOM PII for privacy, existing side panel UI. No WebLLM / MCP / wake word / multi-provider / offscreen work until after demo.
- 2026-09-11 — fix(byok): retire Llama defaults, swap Groq curated list to the free-tier tool-capable set. Root cause: Groq's free tier no longer serves the `llama-3.3-70b-versatile` / `llama-3.1-8b-instant` models we had wired in — save + first tool call now 404s on any key that only has free-tier quota. **Fix** (`src/byok.js`): `DEFAULT_MODELS.groq` → `openai/gpt-oss-20b` (250K TPM, fast, tool-calling on free tier); `KNOWN_MODELS.groq` → `["openai/gpt-oss-20b", "openai/gpt-oss-120b", "groq/compound-mini", "groq/compound"]` — the 20B is the top entry so `populateByokModels()` marks it `(default)` in the Settings dropdown. OpenAI and Gemini lists unchanged. The "provider name → default" guard (`["gemini","openai","groq"].includes(model.toLowerCase())`) still uses exact-match so `openai/gpt-oss-20b` isn't mistakenly cleared. Persistence path is unchanged — the Settings dropdown reads from `BYOK_KNOWN_MODELS`, `onByokSave()` writes `byokProvider`/`byokApiKey`/`byokModel` through `saveSetting` → `SET_SETTING` → `chrome.storage.local`, so users who already saved a llama id will see the picker drop to "Custom…" (preserving their id in the text field) rather than silently swapping to the new default. Follow-up (`src/sidepanel-main.js`): `friendlyError()` example updated to reference `Groq: openai/gpt-oss-20b` in the "pick a tool-capable model" hint. Comment block above `KNOWN_MODELS` still calls out **why** we don't live-fetch (no capability metadata in `/models`), which remains the reason a future contributor shouldn't try to auto-discover these.
- 2026-09-11 — fix(byok): curated tool-capable model dropdown replaces the live /models fetch. Root cause of the reported "`tool calling` is not supported with this model" 400 from Groq: after `444e035` we populated the Settings model picker from each provider's live `/models` endpoint, but **no provider advertises tool-calling capability in that response** — Groq's list happily returns `whisper-*`, `*-guard-*`, `gemma-*` etc. as `active`, and picking any of those blows up in `chatWithTools`. Live-fetching couldn't fix this class of bug. **Fix**: `KNOWN_MODELS` in `src/byok.js` is now the authoritative dropdown source and a curated tool-calling list per provider — Gemini `[gemini-2.0-flash (default), gemini-2.5-flash, gemini-1.5-flash]`, OpenAI `[gpt-4o-mini (default), gpt-4o, gpt-4.1-mini]`, Groq `[llama-3.3-70b-versatile (default), llama-3.1-8b-instant, openai/gpt-oss-120b]`. The top entry is the provider's default (matched to `DEFAULT_MODELS`). "Custom…" option stays — it reveals a text input so users can type any model id when new ones ship, or opt into a non-tool model for text-only work. **Sidepanel rewire** (`src/sidepanel-main.js`): dropped `fetchByokModels`, `liveModelsCache`, `cacheKey`, and the whole `live`-vs-cache dance; new `populateByokModels(provider)` just fills the select from `BYOK_KNOWN_MODELS[provider]`, restoring the saved id if it's in the list, dropping to Custom (preserving the id in the text field) if it isn't, else selecting the default. `renderByok()` calls it on settings open and provider-change; `onByokSave()` no longer force-refetches on key/provider change. Refresh button removed from `sidepanel.html` + `.byok-refresh` + `.byok-model-row` CSS purged. **byok.js purge**: `listModels` and all supporting helpers (`fetchJson`, `REQ_TIMEOUT_MS`, `listGeminiModels`, `listOpenAIModels`, `listGroqModels`, `MODEL_LISTERS`) removed — the whole "live model listing" surface is gone, since it was only used from the sidepanel and reintroducing it would just recreate the bug. Comment in `DEFAULT_MODELS`/`KNOWN_MODELS` block spells out **why** we don't live-fetch (no capability metadata) so a future contributor doesn't try to "improve" it back into the live path. **Also carried over from earlier this session**: `friendlyError()` in `src/sidepanel-main.js` matches the provider 400 pattern (`/tool[-_ ]?call(ing|s)?\b/i` + `/not support/i`) and replaces the raw error with "This model doesn't support tool calling. Pick a tool-capable model in Settings (e.g. Groq: llama-3.3-70b-versatile, OpenAI: gpt-4o-mini)." — still useful as a safety net if a user pastes a custom model that turns out to lack tool support. Persistence: unchanged — `settings.byokProvider`/`byokApiKey`/`byokModel` still live in `chrome.storage.local` via the existing `saveSetting` path.
- 2026-09-11 — feat: SEEING + CONTROLLING wired end-to-end. Root cause of "I can't see" / "I can't control": the composer was sending only text — no page snapshot, no way for the LLM to act. Three commits land the minimum-viable versions.
  - **SEEING** (`6ec7bad`) — new `SNAPSHOT_COMPACT` message type + content-side `collectCompactSnapshot()`. Different shape from the vision-pipeline `SNAPSHOT`: no image, no bboxes, just `{url, title, visibleText:<first 3000 chars of body.innerText>, elements:[{id, tag, role, name, text, selector, ...}]}`. Element selection is interactive-only (button / a[href] / input:not(hidden) / textarea / select / [role=button|link|textbox] / contenteditable). Visibility gate = `offsetParent !== null` + non-zero rect + visibility:visible + opacity > 0. Cap 150 elements. Stable selectors: prefer unique `#id`, otherwise stamp `data-friday-id="fri-N"` on the element itself and return `[data-friday-id="fri-N"]`. Cleared at the start of every re-snapshot so re-invocations don't accumulate stamps. Privacy — password / current-password / new-password / cc-* / one-time-code values collapse to `"(hidden)"`. Long text truncated to 80 chars per element. Verified with 11 jsdom-fixture assertions (URL / title / visible-text filter / element shape / id-preference / password redaction / hidden filter / href capture).
  - **CONTROLLING** (`69f4554`) — content-side `EXEC_TOOL` handler executes six tools: `click(target)` / `type(target, text)` / `scroll(direction, amount?)` / `goto(url)` / `readText(target?)` / `getSnapshot()`. Selectors from the compact snapshot plug straight in as `target`. Errors return `{ok:false, error}` (never throw) so the LLM can recover by picking a different target. Background routes EXEC_TOOL to the content script except `goto` which needs `chrome.tabs.update` in the SW. `byok.js` gains `chatWithTools({provider, apiKey, model, messages, tools, ...})` — different entry point from `callByok()` because the shape is fundamentally different (full messages array in, full assistant message with `tool_calls` out). OpenAI + Groq speak the same body via `chatOpenAICompatible()`; `supportsToolCalling(provider)` returns false for Gemini so the UI falls back to the legacy `runAgent` for that provider. New `src/chatAgent.js` — `runChatTurn({userMessage, history, mode, provider, apiKey, model, onEvent})` is the tool loop: fresh snapshot → build system prompt → POST messages+tools → if `tool_calls` in reply, execute each via EXEC_TOOL, feed results back as `role:"tool"` messages, loop up to `MAX_TOOL_STEPS=8` → return text when the model replies without tool_calls. Emits `{phase:'snapshot'|'model-call'|'tool-call'|'tool-result'|'done'|'error'}` for UI progress. **Side panel rewired**: composer submit picks the tool-calling path when `source=byok && supportsToolCalling(provider)`, falls back to legacy `runAgent` for Gemini/local. Both paths render the same chat scroll (`chat-msg--user` / `--assistant` bubbles + inline `chat-tool` monospace chips between them for each tool call). Status pill shows "Answering (with page context: N elements)" during the model call. Chat history persisted in `chrome.storage.local` under the new `chatHistory` setting key (JSON string of last 40 messages). Restored into the chat view on side-panel boot if non-empty. **+ New chat button** in the run-view header clears history and returns to home. Spinner unwind moved into the finally block of `onSubmitComposer` regardless of success/error/timeout.
  - **VERIFICATION** (this commit) — `test/chat-test.html` — a minimal login-form test page with three built-in scenarios: (1) "What's on this page?" → snapshot-only answer, (2) "Click the login button." → single `click` tool call flips the button green, (3) "Fill the email field with test@ex.com." → single `type` tool call turns the input green. Ground-truth trace at the bottom logs every executed hit + typed value so verification is visual. Also added `window.__fridayVerbose = true` flag — flip it in DevTools and every runChatTurn call logs the system prompt (first 400 chars), every tool call + result, and per-step latency. Simulated the full tool loop against a mock LLM + fake page state (16 assertions across the three scenarios + a bad-selector recovery case + a runaway-loop step-limit case) — all pass. **Known gap**: chat history stores only `{role, content}` pairs, not tool_calls + tool responses, so a restored conversation loses the tool-round-trip context. Fine for the MVP — the system prompt is rebuilt every turn with a fresh snapshot anyway. Follow-up if the LLM starts referring back to prior tool results: extend history persistence to include tool_calls + tool messages in the exact order the API needs.
- 2026-09-11 — fix(byok): live model lists per provider, kill the hardcoded-Groq-model treadmill. Root cause of the recurring 404s: model IDs on Groq turn over every few months (`llama-3.2-*-vision-preview` deprecated Q1 2025 → we shipped `meta-llama/llama-4-scout-17b-16e-instruct` → Groq deprecated that on 2026-07-17 → back to 404). Any hardcoded default is on a countdown. **Fix**: fetch the LIVE model list from each provider's public /models endpoint whenever a key is present. `src/byok.js` gains `listModels({provider, apiKey})` behind a `MODEL_LISTERS` table — Gemini hits `generativelanguage.googleapis.com/v1beta/models?key=…` and filters to models where `supportedGenerationMethods` contains `generateContent` (skips embedding-only models that would confuse users); OpenAI hits `api.openai.com/v1/models` with `Authorization: Bearer`; Groq hits `api.groq.com/openai/v1/models` and drops entries with `active:false` (that's exactly how `llama-4-scout` now appears in Groq's response — visible but deactivated). `fetchJson()` wrapper handles 15s AbortController timeout, pulls the provider's error message out of `error.message`/`message` fields on non-2xx so users see "Invalid API Key" not "500". **UI (`sidepanel-main.js` + `sidepanel.html` + `sidepanel.css`)**: model picker still a `<select>` but now populated live. `fetchByokModels(provider, apiKey, {force})` runs on settings open (if key set), on provider change, and on Save. In-memory cache keyed by `provider:apiKey` so reopening settings doesn't re-hit the API. New round refresh button next to the select — icon-only, disabled during fetch. Status pill shows "Fetching…", "N model(s) available.", or the raw provider error (red tint via `.byok-status--error`). **Deliberate no-silent-fallback**: on fetch failure the dropdown gets ONLY the "Custom…" option — we do NOT auto-populate `KNOWN_MODELS` because those are the exact ids that go stale. User sees the real error and either fixes the key, refreshes, or types a custom id. Fallback `DEFAULT_MODELS` updated to currently-live values (gemini `gemini-2.0-flash`, openai `gpt-4o-mini`, groq `llama-3.3-70b-versatile` for text — vision users pick a llama-4 from the live list). `KNOWN_MODELS` trimmed to a short pre-key preview list only. Verified with a 4-case fixture test: Gemini keeps only generateContent models, OpenAI passes through, Groq drops `active:false`, and empty `data[]` doesn't crash. Composer path unchanged — router still reads `settings.byokModel`, and the belt-and-braces "reject provider-name-as-model" guard from the earlier fix still runs.
- 2026-09-10 — F.3 done (text half): `README.md` at repo root. Covers what it does (sees / redacts / reasons / acts / talks / extends), install-and-load-unpacked instructions, the demo flow (both `demo-signin.html` end-to-end and `executor-test.html` accuracy harness), an architecture ASCII diagram showing side panel ↔ service worker ↔ content-script + the src/ module graph, the runtime deps under `dist/vendor/` and the CSP explanation (`wasm-unsafe-eval` — MV3 forbids `unsafe-eval` so TFJS WASM is the fallback), the locked design decisions, and the honest known caveats (voice input not strictly on-device, local VLM off-by-default, MCP HTTP-only, and — flagged explicitly — **demo video not yet recorded**, which needs a human). Repo-layout tree explains what every file does so future readers can navigate cold. **All 24 tasks across Phases 0–6 + Final are complete except the demo video recording**, which I can't do from the CLI. Every task committed and pushed per the plan's git rules (one commit per task, small and frequent).
- 2026-09-10 — F.1 + F.2 done: **F.1 demo page** — `test/demo-signin.html` is a controlled Acme sign-in card that exercises every detector: an inline SVG avatar (`data:image/svg+xml…` — no network fetch, works offline) drawn as a stylised human face so BlazeFace has an unambiguous target, `<input type="email" autocomplete="email">` + `<input type="password" autocomplete="current-password">` + `<input type="tel" autocomplete="tel">` covering three PII kinds via `type` and `autocomplete` signals, and background copy with `support@acme.example.com` + `+1 (555) 010-5309` for the OCR path. Submit is intercepted (no navigation) and swaps in a green "Signed in — Friday demo complete." marker so the user can visually confirm the executor click landed. A crimson `.demo-note` at the bottom of the page tells the user exactly what to do: open the side panel, click Run privacy scan, then type "Sign in with jane.doe@acme.example.com and password Sup3rSecret" — the end-to-end path (capture → BlazeFace + Tesseract + DOM PII → redact → sanitized payload → route → click Sign in) runs against this page as-is. **F.2 error handling + polish** — `friendlyError(msg)` translates the low-level throws into user-facing lines: `http(s) only` → "This page can't be inspected (chrome:// or extension pages…), open a normal website tab"; `no active tab` → "Click into a website tab"; `API key` errors → "Open Settings and paste a valid key"; `MCP` errors get the "check Settings → MCP servers" suffix. Both the run view and the scan button use this. Composer submit now catches the two most common misconfigurations up-front: **local reasoning without vlmEnabled** (router would try to auto-load a 250 MB VLM without a heads-up — we now stop and tell the user to enable VLM or switch to Cloud) and **BYOK without API key** (already had this). `setComposerBusy(true)` disables the send button + input during a run so the user doesn't stack agent invocations. Observing rows in the trace got a real animated crimson spinner (`fridaySpin` keyframes on a border-based circle) so the "step 3 — capturing" state feels alive instead of frozen. Next: F.3 (README + video — video I can't record, flag that).
- 2026-09-10 — 6.1 + 6.2 done: MCP (Model Context Protocol) client + agent tool-calling. `src/mcp.js` — Chrome extensions can't spawn stdio subprocesses, so the client **only speaks the HTTP transport variant** of MCP: JSON-RPC 2.0 POSTed to a server URL. Methods: `initialize` (protocolVersion `2024-11-05` + client info), `tools/list` (returns tools with `inputSchema`), `tools/call` with `{name, arguments}`. `rpc()` handles AbortController + 15/30s timeouts, HTTP errors, and MCP JSON-RPC `error` field. `MCPClient` maintains `capabilities` + `tools` per server. **`MCPManager`** takes an array of `{name, url, headers, enabled}`, filters out disabled servers, initialises all in parallel via `initAll()` (returns per-server ok/toolCount/error report so the trace can show init failures), and exposes `allTools()` (flat `[{server, tool}]` for the router prompt) + `callTool(server, tool, args)` (dispatch). **6.2 PII scrub on args**: `scrubPii(val)` walks any JSON-shaped input recursively — strings get every `PATTERNS.*` from `pii.js` applied via global `.replace()` with `redactShort()` output; arrays and objects are traversed; other types pass through. The scrub runs **on the client** before args cross the wire, so if the LLM ever included a raw email/aadhaar/PAN/SSN in tool arguments (it shouldn't, since the LLM sees only redacted context — but this is defence in depth), it gets replaced before send. `callTool()` also returns `sentArgs` so the trace can show the user EXACTLY what left the machine. **Router extended**: `route()` and `buildPrompt()` accept `mcpTools`; when non-empty, the prompt gains a new "External tools you may call (MCP)" block with per-tool `server`/`name`/`description`/`schema` (first 120 chars of inputSchema JSON) and the JSON schema in the response spec expands to include `"mcp"` action + `server`/`tool`/`args` fields. `parseAction()` accepts `"mcp"` and coerces those fields. **Agent loop extended**: `runAgent()` takes `mcpServers`, spins up `MCPManager` once (not per-step), emits `{phase:'mcp-init', report}` before the first observation, threads `mcpTools` into every `route()` call. On `action.type === "mcp"` it calls `mcpManager.callTool(server, tool, args)` and returns `{mcp:true, server, tool, sentArgs, content, isError}` as the execResult — the sentArgs field is the audit trail. Errors ("mcp action requested but no MCP servers configured", per-server call failures) surface via the standard error path. **Side panel**: new settings row `#mcpRow` — a JSON textarea (spellcheck off) with format `[{name, url, headers?, enabled?}]`, a save button that JSON-parses + validates every server has a url + pretty-prints back, status pill showing "N server(s) configured." when valid or "Parse error: …" when broken. `parseMcpServers()` handles the JSON string → array conversion; the composer submit passes it into `runAgent`. Trace UI adds an "Call MCP <server.tool>" title for mcp steps so users can see when an external tool was invoked. Because MCP HTTP servers are typically same-origin-per-user or authenticated with a bearer token, `MCPClient` accepts `headers` for auth, and the settings hint calls this out. Full stretch flow: user asks "email these notes", LLM sees Gmail MCP tool in its prompt, emits `{"action":"mcp","server":"gmail","tool":"send_email","args":{...}}`, agent scrubs args (any leaked email/phone → masks), POSTs to the server, gets a receipt back. Next: FINAL polish (F.1 demo task setup, F.2 UX polish + error handling, F.3 README).
- 2026-09-10 — 5.1 + 5.2 + 5.3 done (shared voice module): `src/voice.js` — thin wrapper over `SpeechRecognition` + `speechSynthesis`. **Documented caveat baked into the file**: Chrome's Web Speech Recognition routes audio through Google's speech service, so voice INPUT isn't strictly on-device — reasoning still is (the header toggle continues to gate that boundary correctly), but the voice-to-text path is a known concession of the plan's "Web Speech API STT" choice. TTS uses the local OS voice engine and IS on-device. Exports: `isSttSupported()`, `isTtsSupported()`, `createRecognition(opts)`, `speak(text, {rate, pitch, lang})` (cancels in-flight utterances so the user doesn't get overlaps), `stopSpeaking()`, `startDictation({onInterim, onFinal, onEnd, onError})` for **5.1 one-shot dictation**, and `startWakeWord({phrase="hey friday", onWake, onTask, onError})` for **5.2 wake-word listening**. The wake-word implementation runs `SpeechRecognition` in continuous+interimResults mode, scans every result for the phrase, splits on it and captures the remainder as the task, and **auto-restarts itself on `onend`** because Chrome auto-stops SR after ~30 s of silence — the 100 ms yield-to-event-loop is important, immediate `.start()` from inside `onend` throws in Chrome. `onWake` fires the moment the phrase is heard so callers can play a subtle ack; `onTask` fires when the utterance completes with a non-empty task. **Side-panel mic wiring**: `mousedown/mouseup` implements a manual long-press detector — single click = one-shot dictation (populates composer with interim text, auto-submits on final), long-press or right-click = wake-word toggle. `data-state="listening"` on the mic button drives a crimson pulse via `@keyframes micPulse`; `data-state="wake"` shows a dashed crimson outline. `onFinal` and wake-`onTask` both call the same `onSubmitComposer` code path — no duplicate agent-run logic. **5.3 TTS**: `maybeSpeakFinal(evt)` runs on every agent `stop` event; only speaks in Agent mode (not Chat) so a user typing back and forth doesn't get every reply read aloud. This closes the hands-free loop: user says "Hey Friday, submit the form" → wake word activates → task captured → agent runs (capture → redact → route → click) → TTS speaks the final "Done" message. `stopSpeaking()` exposed for future interruption. Next: Phase 6 (MCP client — stretch).
- 2026-09-10 — 4.4 done: ReAct loop shipped end-to-end, side-panel composer now runs real agent tasks. `src/agent.js` — `runAgent({task, source, config, maxSteps=5, onStep})` = OBSERVE (`runPrivacyPipeline` + `buildSanitizedPayload`) → REASON (`route()` against local or BYOK) → ACT (`sendToBackground(EXECUTE, {action, fid, text})`) in a loop, terminating on `action.type==='stop'|'say'`, on exec error (don't spin on the same failure — surface and stop), or on `maxSteps`. Between steps, a 700 ms `OBSERVE_DELAY_MS` lets the page settle so the next observation catches the post-action DOM. Every phase emits a structured event through `onStep(evt)`: `{phase:'observe',step}`, `{phase:'reason',step,latencyMs,source,model,action,raw}`, `{phase:'act',step,action,execResult,execError}`, `{phase:'stop',step,reason,output}`, `{phase:'error',step,message}` — this is the ONLY surface the UI reads from, so the loop's internal state stays hidden. **Composer wired**: submitting the side-panel input fires `onSubmitComposer`; it reads `settings.reasoningSource` (from the header Cloud/On-Device toggle), passes BYOK config (`provider/apiKey/model`) if source is `byok`, and **short-circuits with an inline error** if source=byok but no API key is set — no hidden failure, user sees exactly what's missing. Guards against re-entry via `agentInFlight`. **Run view** (new `#runView`): task pill at the top, live-streaming trace of steps as they arrive (observe rows are dashed-border placeholders that get **replaced** by the reason step when it lands — no clutter, one row per completed step), each step card shows `Click <code>f-42</code>` / `Type into <code>f-15</code>`, latency + source + model chips in the meta line, and the LLM's `reasoning` in an italic left-border quote so the user can audit *why* it chose that action. Errors get red trim (`.trace-step--error`). Final result renders in a crimson-tinted card at the bottom ("Done. …" for stop, "Stopped. exec failed at step N…" in red for errors, "hit maxSteps=5 without stop" if we time out). Close button returns to the empty state. `escapeHtml()` locks down all user/model text before it hits `innerHTML` (defence-in-depth even though the model output goes through the sanitized payload — the reasoning string isn't sanitized). Full pipeline is now: composer submit → sanitized payload (redacted image + safe DOM) → route → LLM chooses `click|type|scroll|stop|say` with fid + reasoning → executor hits the exact DOM node → repeat. Next: Phase 5 (Voice — Web Speech STT + wake word + TTS).
- 2026-09-10 — 4.3 done: BYOK cloud sources fully plumbed. `src/byok.js` — three adapters behind one `callByok({provider, apiKey, model, prompt, imageDataUrl})` entrypoint: **Gemini** posts to `generativelanguage.googleapis.com/v1beta/models/<model>:generateContent?key=<apiKey>` with `contents[].parts` alternating an `inline_data` image (base64 + mime split via `splitDataUrl()`) and the text prompt, `generationConfig:{temperature:0, maxOutputTokens:512}`. **OpenAI** posts to `api.openai.com/v1/chat/completions` with `Authorization: Bearer` and a multipart user message (`{type:"text"}` + `{type:"image_url", image_url:{url: dataUrl}}` — OpenAI accepts the data URL directly). **Groq** posts to `api.groq.com/openai/v1/chat/completions` with the same OpenAI-shaped body (they're API-compatible). Each returns raw string text — parsing lives in router.js. `DEFAULT_MODELS` per provider: `gemini-2.0-flash` / `gpt-4o-mini` / `llama-3.2-11b-vision-preview` — the fastest cheap vision-capable option in each family, overridable via settings. **Settings gained four keys**: `reasoningSource` (`'local'|'byok'` — the ONE config value that the router's source parameter reads), `byokProvider`, `byokApiKey`, `byokModel`. `SETTING_KEYS`/`DEFAULTS` extended; `background.js`'s `onInstalled` iterates SETTING_KEYS so the four new keys seed cleanly on existing installs too. **Header Cloud/On-Device toggle is now the primary UI for reasoningSource**: clicking flips it between `'local'` and `'byok'`, and `onDeviceOnly` mirrors the state (`true` when local) so any legacy reader still sees a consistent value. Rendering reads `reasoningSource` first, falls back to `onDeviceOnly`. **Settings view gained a "Cloud API key (BYOK)" stacked row** above the VLM opt-in: provider `<select>` (Gemini/OpenAI/Groq), password-input for the key (autocomplete=off, spellcheck=off, hidden by default), text input for model (placeholder shows the default for the currently selected provider — updates as the provider changes), status pill ("No key set." vs "Key set for <provider>." with a green tint via `.byok-status--set`), and a Save button that persists all three fields via SET_SETTING. Copy explicitly tells the user "Redacted screenshot + safe DOM are sent — never raw PII." reinforcing the privacy boundary. Keys live in `chrome.storage.local` — device-scoped, OS-keychain-backed. No passphrase encryption on top (would trade too much UX for a marginal gain against a local-machine threat). Composer's "Select Model" pill is still visual-only — that binds in 4.4 when the send button fires the ReAct loop. Next: 4.4 (ReAct-style loop: plan → tool/action → observe → repeat, with a Stop).
- 2026-09-10 — 4.1 + 4.2 done: Brain Router with pluggable sources. `src/router.js` exports `route({task, payload, source, config})` — the SINGLE entry point every reasoning caller uses, so the executor and the (upcoming) ReAct loop don't know or care whether reasoning came from local VLM or a cloud API. Two sources today: `SOURCES.LOCAL` (SmolVLM via Transformers.js — reuses the same `runInferenceOnUrl()` we shipped in 0.6, gated behind `vlmEnabled`), and `SOURCES.BYOK` (cloud, wired in 4.3 next). Both return the same normalised shape: `{source, raw, action:{type, fid?, text?, reasoning?, confidence?}, meta:{...}}`. Action types: `click | type | scroll | stop | say` — a superset of the executor's actions plus `stop` (task complete or can't proceed) and `say` (verbal-only reply, no page mutation). **Prompt design**: `buildPrompt(task, payload)` shows the model the exact JSON schema up-front, page host+path (URL scrubbed via `buildSanitizedPayload`), an elements list capped at 60 visible items with `fid <tag:type> role=... name="..." value=...` per line, then the user's task, then `"Respond with the JSON only."` — this exact structure is well-known to elicit reliable JSON from small models. Values in the elements list are summarized: `{masked:kind}` for redacted, first 40 chars for plain strings — never leaks PII into the prompt. **Parser**: `parseAction()` extracts JSON from ` ```json ``` ` fences OR bare braces (LLMs love wrapping), grabs the first well-formed `{...}` slice, validates `action` against the allowed set, coerces `fid`/`text`/`reasoning` to strings. On any parse failure it falls back to `{type:'say', text:<raw>, reasoning:'unparseable — treated as free-form response'}` so the ReAct loop can decide whether to stop or retry. **Local branch** auto-loads the VLM via `loadModel({preferWebGPU:true})` on first call so downstream callers don't need to pre-warm — but honours the cached `modelState.model` on subsequent calls (no re-download, per 1.6). **BYOK branch** dynamic-imports `./byok.js` so the router file stays lean and users on the local path don't pay parse cost for cloud plumbing. Both branches attach timings (`inferMs+totalMs+backend` for local, `latencyMs+provider+model` for BYOK) so the ReAct loop can log per-step costs. Next: 4.3 (BYOK settings + Gemini/OpenAI/Groq adapters).
- 2026-09-10 — 3.1 + 3.2 + 3.3 done in one landing (they share plumbing): content-side executor and resolver + verification harness. **3.1 executor.** `content.js` now maintains `currentFidMap: Map<fid, HTMLElement>` — rebuilt on every `collectSnapshot()` so the SAME element the snapshot surfaced is what EXECUTE clicks. No pixel guessing, no coordinate translation, no re-search. New `EXECUTE` message type (`src/messaging.js`) with payload `{action, fid, text?, options?}`. Handler in content.js dispatches on `action`: `click` → `scrollIntoView({block:"center"}) + el.click()` (native dispatch triggers bubbling listeners AND `<label for>` activation, which hand-rolled MouseEvent misses), `type` → focus + set value via `Object.getOwnPropertyDescriptor(HTMLInputElement.prototype, "value").set.call(el, text)` so **React and other library-controlled inputs** update their internal state (bare `el.value=` doesn't trigger React re-render), then fire `input` + `change` events; for `contentEditable` it uses `document.execCommand("insertText")` — with support for `options.append` to concatenate instead of replace. `focus` and `scroll` are standalone actions. `assertReady()` checks the fidMap is populated AND `el.isConnected` (element still in the document — throws if the DOM changed since snapshot). `isDisabled()` checks both `el.disabled` and `aria-disabled="true"`. Returns `{ok, action, fid, tag, ms, chars?}` per call for telemetry. **3.2 resolver.** `RESOLVE` message with payload `{intent, role?}`. `resolveIntent()` walks the fidMap, computes each element's accessible-name (already available via `accessibleName(el)`), and scores intent-vs-name matches: exact match = 100, `name.includes(q)` scaled by length delta (60 minus |Δlen|), reverse `q.includes(name)` = 40, +5 bonus for interactive roles (button/link/textbox), returns `{matches: top5, best}`. Optional `role` filter narrows to a role class. Deliberately kept small — the Phase-4 LLM will handle harder disambiguations by seeing the whole snapshot; this is the fast "obvious match" path. **3.3 verification.** New `test/executor-test.html`: 18 controls across sizes and types — full-sized buttons ("Submit Order", "Continue", "Delete Account", "Cancel"), tiny buttons at 9px font ("×", "Close", "Edit", "Save" — deliberately ambiguous with "Save Draft"), inputs with autocomplete tokens (Email/Password/Full name), text links (Learn more / Privacy policy / Terms of service), and a Next/Previous/Save Draft/Publish quad. Each control has `data-tid` and click/input handlers that populate `window.__fridayHits` and `window.__fridayTyped` and turn the element green on hit — so the accuracy check has ground truth. Popup gained an intent input, "Resolve" / "Click best" / "Type into best" buttons for ad-hoc testing, and a "Verify executor" button that iterates 18 preset `{intent, expectedName, action}` cases against the RESOLVE + EXECUTE endpoints and logs both **resolve accuracy** (did we land on the right element by name?) and **exec accuracy** (did click/type actually succeed?). Also, ensured `ensureSnapshot()` always runs a CAPTURE_TAB first so the executor never fires against a stale fidMap. Coordinate-space guarantee: because we click via `el.click()` on the actual live node, tiny 12×12 buttons hit as reliably as full-sized ones — the accuracy story from here is **resolver quality**, not click precision. Next: Phase 4 (Brain Router + reasoning).
- 2026-09-10 — 2.6 done: `buildSanitizedPayload(receipt, {keepUrl})` in `src/pipeline.js` — this is the final privacy-boundary function. Takes a `runPrivacyPipeline()` receipt and produces the exact shape a Phase-4 cloud call will send. **Image**: swaps in `redaction.dataUrl` (the blurred/masked canvas output) — the raw `capture.screenshot` never appears in the payload if a redaction ran. **Elements**: walks `capture.elements`, cross-references each `fid` against `capture.pii.hits`, and for any flagged element replaces `value` with `{masked:true, kind, length}` (preserving the character-count signal that's useful for the LLM without leaking the actual text). Elements that were already masked at capture time (password/sensitive-autocomplete) get the same shape descriptor but annotated with the detected kind. Non-PII elements pass through unchanged — their role/name/bbox is exactly the signal the model needs for control tasks. **URL scrubbing**: `page.url` defaults to `{host, path}` only — query strings + hashes commonly carry session tokens and tracking IDs that shouldn't cross the wire; `keepUrl:true` opts back into full URL when the task demands it. **Receipt**: attaches `{total, perKind, perSource, regionsApplied}` (aggregate counts, no raw values) so the model sees WHAT was redacted without seeing the values, and downstream code can log/audit. **Meta**: `{capturedAt, totalMs, redactMs, regionCount}` for telemetry. Popup gained a "Safe payload" button that runs the full pipeline end-to-end and logs the payload shape + a **safety check**: greps element values with an email-regex and warns if any raw PII leaked through — sanity-tests the boundary. Screen preview updates to the redacted image so you can visually verify. Nothing about the raw capture ever reaches this payload; the redactor's dataUrl and the masked-element list are the ONLY surfaces of information about the page. This is the piece Phase 4 (Brain Router) will hand to the LLM. Next: Phase 3 (Control layer — DOM-anchored executor).
- 2026-09-10 — 2.5 done: Privacy receipt UI shipped in the side panel. `src/pipeline.js` — single-call `runPrivacyPipeline({mode, onPhase})` that chains `sendToBackground(CAPTURE_TAB)` → decode screenshot into an `Image` for dimensions → `Promise.all([detectFacesFromDataUrl, runOcrOnDataUrl])` in parallel → `collectRegions()` → `redactImage()` → `summarize()` counts per-kind and per-source. Returns `{capture, faces, ocr, regions, redaction, counts:{total, perKind, perSource, domHits, faceCount, ocrHits}, totalMs}` — a self-contained receipt suitable for both rendering and (Phase 4) attaching to an outbound request. Exports `KIND_LABEL` (human strings for each PII kind: "Passwords", "Emails", "Aadhaar numbers", "Faces", etc.). **Side-panel receipt card** (per UI DESIGN SPEC — the empty state now shows a "Run privacy scan" pill that lands users on the receipt): shield badge in `--accent-soft`, "ON-DEVICE ONLY" eyebrow, close-X, big total number ("12 items redacted before send"), per-kind list with crimson dot + label + tabular count (sorted by count desc), redacted screenshot preview below, meta line "DOM: 4 • Faces: 1 • OCR: 7 • Total: 823 ms • example.com", "Scan again" primary button. Empty-state receipt list falls back to "No PII detected on this page." Wiring: `runScan()` disables both scan buttons, streams phase labels ("Capturing screen…", "Detecting PII (faces + OCR)…", "Redacting…") into the button text via `onPhase`, catches errors and surfaces them inline (`Scan failed — <msg>`, auto-clears after 4s). `openSettings()` teaches the receipt view about state — closing settings returns to receipt if one exists, else to the empty state. Escape closes the receipt (same pattern as settings). Reuses the `.btn-primary` and existing pill styles for visual consistency; the receipt list rows sit on `--bg-soft` cards inside the outer white card. No new deps, no CSP change — all detection modules were already imported through `pipeline.js` which is dynamically imported at pipeline call time (only pays the TFJS/Tesseract load cost when the user clicks Run privacy scan). Next: 2.6 (build sanitized payload = redacted image + safe DOM structure, wire-ready for Phase 4).
- 2026-09-10 — 2.4 done: `src/redact.js` — Canvas-based on-device redactor. `collectRegions({dom, faces, ocr, viewport, imageWidth, imageHeight, pad})` normalises hits from all three detectors into a single `[{bbox, kind, source}]` list, handling the **coordinate-space mismatch** cleanly: DOM bboxes come from `getBoundingClientRect()` in CSS pixels, but `chrome.tabs.captureVisibleTab` returns a device-pixel image, so we compute `sx = imageWidth/viewport.width, sy = imageHeight/viewport.height` once and scale DOM boxes by it. Face + OCR bboxes are already in image pixels and pass through untouched. Faces get extra 12-16 px of pad since BlazeFace's tight box crops ears/hair. `expand()` handles the pad + clamps to zero. `redactImage({imageSource, regions, mode, blurPx})` decodes the source (accepts HTMLImageElement / HTMLCanvasElement / ImageBitmap / URL / dataURL), draws it once into a canvas at natural dimensions, then paints over each region via `applyMask()`. Three modes: `MASK` (solid black `fillRect` — fastest, strongest signal), `BLUR` (default, `ctx.filter = "blur(Npx)"` inside a `ctx.clip()` rect — smooth gaussian localised to the region), `MOSAIC` (downscale to a tiny temp canvas then upscale with `imageSmoothingEnabled=false` — pixelates for extra "clearly redacted" affordance). All three finish in a handful of ms even on 2880×1800 screenshots because we only redraw within clipped regions. Returns `{dataUrl, width, height, regionsApplied, regionsGiven, redactMs}`. Popup gained a "Redact" button: captures the screen (or reuses `lastCapture`), fires BlazeFace + Tesseract in **parallel** via `Promise.all` (unrelated work, no reason to serialise), combines all three detector outputs through `collectRegions`, applies `BLUR` at 22 px, and swaps the preview image to the redacted version. `lastCapture` (full CAPTURE_TAB payload — includes `pii`, `viewport`, `elements`) is now stashed on `onCapture` so redact can pull DOM regions without re-hitting the tab. Logs `[redact] faces=N ocr.words=M ocr.pii=K`, `[redact] regions=T (dom=… faces=… ocr=…)`, and `[redact] applied=A/G redactMs=… totalMs=…` — cleanly instruments each pipe. Next: 2.5 (Privacy receipt UI).
- 2026-09-10 — 2.3 done: OCR + text-PII pipeline with Tesseract.js. Added `tesseract.js` dep; extended `scripts/build.mjs` to copy `tesseract.min.js` (UMD, sets `window.Tesseract`), `worker.min.js` (the Tesseract Web Worker), and the entire `tesseract.js-core/` folder (`tesseract-core.wasm`, `tesseract-core-simd.wasm`, `tesseract-core-relaxedsimd.wasm`, plus their LSTM variants) into `dist/vendor/tesseract/{,core/}` — runtime picks the fastest variant the browser supports. Only the language data (`eng.traineddata`, ~10 MB) fetches from `https://tessdata.projectnaptha.com/4.0.0` on first use and is Cache-Storage-cached after (same pattern as BlazeFace weights and Transformers.js). `src/ocr.js` lazy-loads the UMD, calls `Tesseract.createWorker("eng", 1, {workerPath, corePath, langPath, logger})` — `oem=1` selects LSTM-only (faster, smaller than the legacy+LSTM combo) — and caches the worker on `window.__fridayTesseractWorker` so repeat calls reuse it. `runOcr(imageSource)` recognises via `worker.recognize()` and returns `{text, words:[{text, confidence, bbox:{x,y,w,h}}], textPii:[…], ocrMs}`. Bounding boxes are converted from Tesseract's `{x0,y0,x1,y1}` corner form to our common `{x,y,w,h}` shape. **PII detection reuses `src/pii.js` PATTERNS** — no duplicate regex definitions. Per-word scan first (catches whole-token PII like email/aadhaar/PAN/SSN with bbox precision for later blurring), then a full-text sweep for kinds not seen per-word (catches PII Tesseract split across whitespace, like phone numbers with spaces — `bbox:null` and marked `source:"ocr-fulltext"` so the redactor knows this hit lacks region info). Every hit's `evidence` runs through `redactShort()` so the raw PII never leaves the OCR module. `terminateTesseract()` exposed for callers who want to reclaim the ~4 MB worker memory. Popup gained a "Run OCR" button that operates on `lastScreenshotDataUrl` (the same buffer the Detect-faces button uses) and logs word count, chars, ocrMs, PII count, and the first 6 PII hits with kind + bbox + evidence + source. Next: 2.4 (Canvas mask/blur over every detected region, on-device).
- 2026-09-10 — 2.2 done: BlazeFace face detection wired against the captured screenshot. Added deps `@tensorflow/tfjs` + `@tensorflow-models/blazeface` + `@tensorflow/tfjs-backend-wasm` (WASM fallback so we don't need `'unsafe-eval'` — MV3 forbids that in extension_pages CSP, only `'wasm-unsafe-eval'` is allowed which we already have). Extended `scripts/build.mjs` to copy: TFJS UMD (`tf.min.js`, 1.4 MB), TFJS WASM backend UMD (`tf-backend-wasm.min.js`) + all three `.wasm` binaries (plain, SIMD, threaded-SIMD) into `dist/vendor/tfjs/`, and BlazeFace UMD (`blazeface.min.js`, 10 KB) into `dist/vendor/blazeface/`. **Deliberately UMD, not ESM**, because `@tensorflow-models/blazeface` ESM has bare specifier imports (`import * as t from "@tensorflow/tfjs-converter"`) which won't resolve in the browser without an import map — the UMD path avoids that pit. `src/faces.js` lazy-loads all three via `document.createElement('script')` on first call (dedup'd via `data-friday-vendor` attr) so the 1.4 MB TFJS cost only lands when the user actually invokes face detection, not at side-panel boot. Load order: TFJS core → try `setBackend('webgl')` (fast on any GPU); on failure, inject the WASM backend script, `setWasmPaths(dist/vendor/tfjs/)`, `setBackend('wasm')` → BlazeFace UMD → `blazeface.load()` (fetches ~200 KB weights from Google's model bucket on first use, browser Cache Storage handles reuse — no CSP tweak needed since our custom CSP only pins `script-src`+`object-src`, leaving `connect-src` unrestricted). Result cached on `window.__fridayBlazeface = {model, backend, loadMs}` so the second call is a no-op. `detectFaces(imageSource)` accepts any BlazeFace input (HTMLImageElement, ImageBitmap, canvas, ImageData) and returns `[{box:{x,y,w,h}, prob, landmarks:[{x,y}…]}]` — six landmarks per face (both eyes, ears, nose, mouth). `detectFacesFromDataUrl()` wraps a data-URL screenshot in an `<img>` then runs detection — used by the popup smoke button. Popup gained a "Detect faces" button; `onCapture` now stashes the last screenshot dataURL to `lastScreenshotDataUrl` so the button can operate on it. Logs `[faces] runtime=webgl|wasm loadMs=…`, `[faces] detected=N detectMs=… totalMs=…`, and per-face `box=x,y WxH prob=… landmarks=6`. Kept out of the service worker on purpose — no WebGL in SWs, and the surface (popup/side panel) is where the captured image is displayed anyway. Next: 2.3 (OCR text-PII with Tesseract.js).
- 2026-09-10 — 2.1 done: `src/pii.js` — pure detector (no DOM access, safe to import in the service worker) that operates on the structured element list content.js already produces. Exports `PII_KIND` (password, email, name, phone, cc, aadhaar, pan, ssn, otp, dob), `PATTERNS` (regexes for email, aadhaar `\b\d{4}[\s-]?\d{4}[\s-]?\d{4}\b`, PAN `\b[A-Z]{5}\d{4}[A-Z]\b`, SSN, CC 13–19 digits with optional separators, Indian mobile `\b(?:\+?91[-\s]?)?[6-9]\d{9}\b`, US phone), `detectElementPii(el)` per-element, and `detectDomPii(elements)` bulk. Detection combines four signals per element, ordered high→low precision: (1) input `type` (`password`/`email`/`tel`), (2) `autocomplete` attribute against a spec-token map (`current-password`, `new-password`, `one-time-code`, `email`, `name`, `given-name`, `family-name`, `cc-number`, `cc-csc`, `tel`, `bday`, …) — autocomplete can be a token list like `"shipping given-name"` so we split and check each token, (3) label/id keyword regex list (aadhaar/uidai, pan card, SSN, DOB, CVV, OTP, etc.), (4) value-content regex only when `value` is a plain string (skips the `{masked:true}`/`{truncated:true}` shape descriptors the snapshot emits for sensitive fields). Each hit is `{kind, source: "type"|"autocomplete"|"label"|"value", evidence}`; `redactEvidence()` turns matched raw values into character-shape masks (`"foo@bar.com"` → `"f***@bar.com"`, `"1234-5678-9012"` → `"****-****-****"`) so the receipt can show what kind was found without echoing the PII back through the pipe. `addHit` suppresses duplicate `(kind, source)` pairs on the same element. `content.js` extended to include `autocomplete` in the snapshot for inputs/textareas (lowercased). `background.js` imports `detectDomPii`, calls it after CAPTURE_TAB's `Promise.all`, and returns `{...snap, pii: {hits, counts, total}}` on the response. Popup Capture button now logs `[pii] total=N kind1=x kind2=y …` and the first 5 hits with their `{fid, tag:type, name, kinds}` — first smoke path for the receipt UI that lands in 2.5. Zero new deps, zero new permissions. Next: 2.2 (BlazeFace face detection on the captured screen).
- 2026-09-10 — 1.6 done: moved the local-VLM loader out of `src/popup-main.js` into a new shared `src/model.js` (single source of truth for both the dev popup and the side panel). Module exports `MODEL_ID`, `detectWebGPU()`, `makeProgressCallback(onProgress)` (aggregates loaded/total across all model files — vision_encoder + embed_tokens + decoder — and reports `{loaded, total, pct, mbLoaded, mbTotal, file, status}` on every Transformers.js progress event), `loadModel({preferWebGPU, onProgress})` (WebGPU q4f16 → WASM q4 fallback, browser Cache Storage handles reuse — second load prints `cached (no download)` with `downloadedBytes=0`), and `runInferenceOnUrl()` (unchanged behavior — used by the popup's Run-on-sample). Env setup (`env.backends.onnx.wasm.wasmPaths`, `allowLocalModels=false`, `useBrowserCache=true`) runs once at module import via `new URL('../dist/vendor/transformers/', import.meta.url)`, so both entry points resolve the vendor path correctly. Popup is now a thin dev harness that re-imports `state`/`loadModel`/`runInferenceOnUrl` and prints the same `[load] backend=… loadMs=… cached|downloaded=<N>MB` line as before. **New: side-panel opt-in.** `SETTING_KEYS`/`SETTING_DEFAULTS` gained `vlmEnabled: false`; background's onInstalled + GET_SETTINGS iterate over `SETTING_KEYS` so the new key is backward-compatible with existing installs. New settings row `settings-row--stacked` (`#vlmRow`) with: title "Local VLM (optional)", hint that references gate 0.7, a rounded switch (`.switch`) bound to `vlmEnabled` via `SET_SETTING`, and an expandable body (`#vlmBody`) that reveals the status line + `<progress id="vlmProgress">` + `#vlmDownloadBtn`. Download flow runs `detectWebGPU()` → `loadModel({preferWebGPU, onProgress})` and updates the progress bar + status line on every progress event (`Downloading… X.X / Y.Y MB (N%)`); on completion the status flips to `Loaded on <backend> in Xs (cached (no download)|downloaded X MB)` and the button relabels to "Reload" so a second click confirms the cache-hit path. CSS: added the stacked-row layout, a compact `.switch` component (crimson accent on `:checked`), themed `<progress>` (crimson value bar on `--bg-soft` track), and a `.btn-primary` pill in the send-button palette. Boot wiring: `openSettings()` now calls `renderVlmToggle()` and DOMContentLoaded calls `wireVlm()` so the switch + button events are bound. Note: not moved to the service worker despite the earlier "into BG" note — WebGPU + Transformers.js in an MV3 service worker has known lifecycle issues (SW can idle mid-download), and the side panel doesn't dismiss on outside click, so the "survives popup close" concern doesn't apply. Next: 2.1 (DOM PII detector).
- 2026-09-10 — 1.5 done: `CAPTURE_TAB` BG handler runs `chrome.tabs.captureVisibleTab(windowId, {format:'png'})` and `sendToTab(tab.id, SNAPSHOT)` **in parallel** via `Promise.all` (screenshot is a service-worker Chrome API; snapshot is a content-script trip — no reason to sequence them), returns `{tabId, screenshot dataURL, screenshotBytes, captureMs, page, viewport, elements, elementCount, totalScanned, capturedAt}`. Content-side `collectSnapshot()` queries a curated `CAPTURE_SELECTOR` (`a[href], button, input, textarea, select, label, img[alt], h1-h6, [role], [tabindex], [contenteditable]`), computes per-element `{fid, tag, role, name, bbox {x,y,w,h in viewport coords matching the screenshot}, visible, type?, id?, href?, value?, disabled?}`. Role via `implicitRole()` for common tags then `role` attribute override. Accessible name via a compact ARIA-ish cascade: aria-labelledby → aria-label → associated `<label for>` / wrapping label / placeholder → button/anchor textContent → img alt → submit-button value → title. Visibility filter: non-zero bbox, in-viewport, `display!=none && visibility!=hidden && opacity>0`. **Privacy hard-line even at Phase 1:** `<input type="password">` and inputs with sensitive `autocomplete` tokens (`current-password|new-password|cc-number|cc-csc|one-time-code`) emit `{masked:true, length}` never the value; long text values truncate to 200 chars with `{truncated:true, length, sample}`. Popup got a "Capture" button — renders the PNG inline and logs viewport, element count/total scanned, screenshot KB, capture time, and the first 3 elements with role/name/bbox. All from the existing `activeTab` + `scripting` permissions — no new manifest entries needed. Next: 1.6 (first-run model download progress + cache/reuse — mostly plumbing since Transformers.js already does the download+cache; this task moves it out of the popup into BG so the side panel can trigger it and the download survives popup close).
- 2026-09-10 — 1.4 done: `content.js` at repo root — a self-contained classic script (not a module, since `chrome.scripting.executeScript` files can't `import`) that guards on `window.__fridayContentLoaded` to make injection idempotent (avoids duplicate `sendResponse` warnings on re-inject) and installs a `chrome.runtime.onMessage` listener responding to `CONTENT_PING` with `{url, title, readyState, nodeCount, formCount, inputCount, receivedAt, echo}`. `MSG` constants inline-duplicated from `src/messaging.js` with a "keep in sync" comment — the alternative (importing) doesn't work for injected files. `background.js` gained `isInjectableUrl()` (http/https gate — blocks chrome://, chrome-extension://, view-source:, etc.) and `ensureContentInActiveTab()` which queries the active tab and runs `chrome.scripting.executeScript({ files: ['content.js'] })`; new `CONTENT_PING` handler chains ensure → `sendToTab(tab.id, CONTENT_PING)` → returns the content payload plus `tabId` + `tabUrl`. Popup got a third smoke button "Ping content" that calls `sendToBackground(CONTENT_PING)` and logs the round-trip + page snapshot. No new permissions needed — `activeTab` + `scripting` already declared. Next: 1.5 (screen capture + DOM/a11y snapshot).
- 2026-09-10 — 1.3 done: header controls fully behavioral. `src/sidepanel-main.js` loads settings via `GET_SETTINGS` on boot, seeded with `SETTING_DEFAULTS` if the BG round-trip fails. **Mode pill:** now a `<button>` with `aria-haspopup="menu" aria-expanded=...` → a `.menu` dropdown containing Chat/Agent items (`role="menuitemradio"`). Click toggles the menu, item-click updates the pill label, persists via `SET_SETTING(mode)`, marks the selected item with `aria-checked="true"` (rendered as a crimson dot). Outside click + Escape close the menu. **Cloud/On-Device toggle:** click flips `data-on-device`, persists `onDeviceOnly` boolean; CSS drives the thumb slide + active-side crimson tint entirely from the data attribute. **Settings gear:** click swaps `emptyState` → `settingsView` in the body; Back button + Escape returns. Settings view is a real card list showing current Mode and Reasoning source values (auto-synced when they change elsewhere) plus a dashed "More settings coming soon." row — this is the stub the plan asked for. All state is written through the BG router so any future surface (popup, content script) sees the same values. Next: 1.4 (content script skeleton).
- 2026-09-10 — 1.2 done: Chrome side panel is now the primary UX. Manifest gained `"side_panel": { "default_path": "sidepanel.html" }` + `"sidePanel"` permission; `default_popup` removed from `action` so clicking the toolbar icon opens the side panel (via `chrome.sidePanel.setPanelBehavior({ openPanelOnActionClick: true })` in `background.js`, re-applied every boot since it doesn't survive service-worker restarts). New `sidepanel.html` + `sidepanel.css` built to the LOCKED UI DESIGN SPEC: light theme (`--bg #fff`, `--bg-soft #f7f7f8`, `--text #111114`, `--accent #ff2e63`), rounded corners (`--radius-lg 18`), thin header border, system-ui stack. Header row = crimson spark mark + "Friday" bold, centered "Chat" pill with chevron, then Cloud↔On-Device slide toggle (thumb slides across cloud/monitor SVGs, active side turns crimson, default on-device=true) + gear icon button. Body = centered empty state with a large crimson spark SVG, `"Hey! I'm Friday"` (Friday in `--accent`), grey two-line subtitle. Footer = composer card: text input with the spec's placeholder, then a bottom row with "+" pill, "Select Model" pill, mic pill, and a black circular send button. `src/sidepanel-main.js` for now just calls BG `PING` to confirm messaging works from the side-panel context (all control wiring lands in 1.3). Popup remains reachable directly at `chrome-extension://<id>/popup.html` for dev debugging. Note: `design/friday-ui.png` not created — no reference mockup was supplied; built from the text spec. Next: 1.3.
- 2026-09-10 — 1.1 done: added `background.js` (MV3 service worker, `"type":"module"`) with a promise-based message router that dispatches on `type` via a handlers table, always replies `{ok, data|error, requestId}`, and `return true`s the channel so async handlers can respond. Shared surface in `src/messaging.js`: `MESSAGE_TYPES` (`PING`, `GET_SETTINGS`, `SET_SETTING`), `SETTING_KEYS` (`mode`, `onDeviceOnly`) + `SETTING_DEFAULTS`, `sendToBackground(type, payload)` and `sendToTab(tabId, type, payload)` — both wrap `chrome.*.sendMessage` in a `crypto.randomUUID`-tagged promise and translate `runtime.lastError` / `ok:false` into thrown Errors. Handlers implemented: `PING` echoes payload + timestamp, `GET_SETTINGS` returns stored keys merged with defaults, `SET_SETTING` validates key is in `SETTING_KEYS` before writing. `onInstalled` seeds defaults so `GET_SETTINGS` is stable from first boot. Manifest gained the `background` block. Popup got two smoke buttons ("Ping BG", "Read settings") that call `sendToBackground` and log the round-trip time + response — proves the routing end-to-end. Next: 1.2 (side-panel UI to spec).
- 2026-09-10 — 0.7 **CLOSED — VLM proven, deferred as optional fallback**. Final data: WebGPU 104s first-token (interrupted); WASM 115s first-token, correct answer but loops ("sign in page has two input fields, email and password" repeated). Verdict: local in-browser VLM is functionally correct on this laptop (reads screens accurately) but ~100× too slow for live interaction. **Locked direction:** on-device core = DOM parsing + accessibility tree (control) + BlazeFace + Tesseract.js (PII detection/redaction), millisecond-scale, no local VLM on the critical path. Reasoning routes via BYOK (Gemini Flash / OpenAI / Groq) through the Brain Router; redaction always runs on-device before any API call. Local VLM stays in the repo as an optional off-by-default fallback for strong-GPU users — no further performance chasing on this hardware. LOCKED DESIGN DECISIONS updated accordingly. Beginning Phase 1: 1.1 background service worker + message routing → 1.2 side-panel UI to spec → 1.3 header controls.
- 2026-09-10 — 0.7 WASM numbers + preprocess optimization: user run — WASM `[load] loadMs=55723 downloaded=255.1MB` (first-time q4 download), `[infer] inferMs=118739 firstTokenMs=117689 chunks=5 timedOut=false`, output `"Sign in to Acme."` — real answer, but 118s is unusable. Per-chunk after first token was ~200ms so decode itself is fine; the ~117s cost is entirely in the FIRST forward pass (vision encoder over 512×512 → ~1000+ image tokens, plus prefill). **Optimization committed** before the next test: (1) resize input to 384×384 via `RawImage.resize()` before the processor — ~2.8× fewer image tokens; (2) split-timing — now logs `preprocess=Xms promptTokens=N imageSize=384` separately from `inferMs`/`firstTokenMs` so the next report pinpoints where the seconds go. Awaiting one more WASM run to decide: <30s → ship WASM; still >60s → invoke the plan's fallback ("lean harder on detectors") and mark VLM as a background/fallback path, since DOM+accessibility is the primary control layer per the locked design decisions anyway.
- 2026-09-10 — 0.7 WebGPU numbers (verdict: unusable on Intel gen-12lp): user run — env=Transformers.js 3.8.1; WebGPU `vendor=intel arch=gen-12lp`; `[load] backend=webgpu loadMs=810` (cached; downloaded=183.5MB line is a cache-hit false positive in the byte tally — Transformers.js emits `progress` even for Cache Storage responses; cosmetic, will fix during Phase 1); `[infer] inferMs=104191 firstTokenMs=104188 chunks=2 timedOut=true`; `[infer.out] "This"`. The 60s watchdog fired but `InterruptableStoppingCriteria` only checks between forward passes — the runtime was stuck inside a single WebGPU submit for the first decode step, which is textbook Intel-iGPU + fp16 SmolVLM behavior (shader-compile / fp16 emulation stall). Awaiting WASM comparison numbers before choosing: default-to-WASM (if <15s) OR swap model / lean on detectors (if also slow).
- 2026-09-10 — 0.7 hang diagnosis + fixes: user report — cache works (`loadMs=1025`, `cached=0.0 MB`), PNG decodes cleanly, but inference hangs indefinitely with no `[infer.out]`. Suspected cause: first-inference WebGPU shader compile on Intel iGPU can take 30–60s and previously we had no visibility. Fixes: (1) `MAX_NEW_TOKENS 192 → 64`, greedy already; (2) hooked `TextStreamer(processor.tokenizer, { skip_prompt, skip_special_tokens, callback_function })` — logs `[infer] first-token in Xms` on first chunk, updates the status line every 4 chunks (`generating… N chunks, Xs`), so a silent stall is now visible; (3) real interrupt via `InterruptableStoppingCriteria` — 60s `setTimeout` calls `.interrupt()`, `generate()` returns cleanly, we log `[infer] TIMEOUT after 60s` and still print whatever the streamer accumulated; (4) added "Force WASM" checkbox next to Test AI so the same inference can be benchmarked on CPU vs GPU without touching code — unchecked→WebGPU, checked→WASM. Log line expanded: `backend inferMs firstTokenMs chunks timedOut`. Awaiting user re-run for the numbers.
- 2026-09-09 — 0.7 partial (first-run numbers in): user report — WebGPU available, model loaded on `webgpu` in ~43000 ms (first-run download); inference FAILED with "The source image could not be decoded" because the popup's decoder rejects SVG blobs. **Fix committed:** replaced `assets/sample-screen.svg` with a real `assets/sample-screen.png` (512×512, rendered offline via PowerShell + System.Drawing — bundled binary, no build step). Loader simplified to `RawImage.read(url)` (Transformers.js handles PNG fetch + decode + normalization end-to-end). Added `state.downloadedBytes` tally driven by the shared progress callback — after a fully-cached load no `progress` events fire, so the counter stays 0 and the `[load]` line now prints `cached=0.0MB` vs `downloaded=<N>MB` for unambiguous first-run vs cached distinction. Awaiting user re-run for cached load time + inference numbers + generated text.
- 2026-09-09 — 0.6 code written (numbers pending 0.7): added `assets/sample-screen.svg` (mock sign-in card: title, email input + placeholder, password input + placeholder, primary "Sign in" button, "Forgot password?" link). New "Run on sample" button in `popup.html` (disabled until model loads). `runInference()` in `src/popup-main.js`: fetches the SVG → `createImageBitmap` resizes to 512×512 → `OffscreenCanvas.getImageData` → drops alpha to RGB → wraps in `RawImage`; builds `messages` with image + `"Describe this screen and list buttons and input fields"`, calls `processor.apply_chat_template`, `model.generate({ max_new_tokens: 192, do_sample: false })`, trims prompt tokens off the front of the generated ids before `batch_decode`. Logs `backend`, `loadMs`, `inferMs`, and the decoded text. Since I can't drive a Chrome extension from bash, the real numbers land in 0.7 (user runs it and reports). Next: 0.7 GATE.
- 2026-09-09 — 0.5 done: Test AI now loads `HuggingFaceTB/SmolVLM-256M-Instruct` via `AutoProcessor` + `AutoModelForImageTextToText` (verified both symbols present in vendored `transformers.min.js`; verified ONNX weight paths + sizes on the HF hub before committing dtype choice). WebGPU preferred with `dtype: 'q4f16'` (~188 MB total: vision_encoder 55 MB + embed_tokens 56 MB + decoder 77 MB); on failure falls back to WASM with `dtype: 'q4'` (~262 MB). Aggregated download progress across all model files drives the popup progress bar and status line; final line logs `backend=… loadMs=…`. Browser Cache Storage handles reuse — no re-download on second click. **Caveat for testing:** Chrome extension popups close on outside click, tearing down downloads mid-flight; keep the popup open (or right-click extension icon → "Inspect popup") for the first-run test. Inference (0.6) will consume `state.processor` + `state.model`. Next: 0.6.
- 2026-09-09 — 0.4 done: Test AI button now runs `detectWebGPU()` in `src/popup-main.js` — checks `navigator.gpu`, calls `navigator.gpu.requestAdapter()`, prints `AVAILABLE` (with vendor/architecture/device from `adapter.info`) or `NOT available` (with reason: no `gpu`, null adapter, or thrown error). Sets a matching status line so the 0.5 fallback path is prewired. Next: 0.5 (download+load small VLM on WebGPU, WASM fallback).
- 2026-09-09 — 0.3 done: added `@huggingface/transformers@^3.0.0` via `package.json` + `npm install`; wrote `scripts/build.mjs` which copies `node_modules/@huggingface/transformers/dist/*` → `dist/vendor/transformers/` (JS + ORT WASM), so no CDN at runtime; moved popup entry to `src/popup-main.js` as an ES module that imports from the local vendor path, sets `env.backends.onnx.wasm.wasmPaths` to that folder, disables `allowLocalModels`, enables `useBrowserCache`, and logs proof-of-load on popup open; `popup.html` now loads `src/popup-main.js` with `type="module"` (old flat `popup.js` deleted). Manifest CSP updated: `script-src 'self' 'wasm-unsafe-eval'` so ORT WASM can init. Dev workflow: `npm install && npm run build`; `dist/` gitignored per plan. Next: 0.4 (WebGPU detect on Test AI click).
- 2026-09-09 — 0.2 done: added `manifest.json` (MV3, name "Friday", perms `activeTab`/`scripting`/`storage`, `host_permissions: <all_urls>`, popup action), `popup.html` (Test AI button, live status, progress bar, output area — minimal light theme; the pretty side-panel UI comes in 1.2), `popup.js` (button wiring, status/progress helpers, placeholder progress tick — WebGPU + model load land in 0.4–0.6). Next: 0.3 (bundle Transformers.js offline).
- 2026-09-09 — 0.1 done: renamed plan file to `FRIDAY-PLAN.md`, `git init`, added remote `origin` → https://github.com/saurabh-v12/Friday-extension.git, wrote `.gitignore` (node_modules, dist, build, IDE junk, logs, .env), first commit + push `-u origin master`. Next: 0.2 (MV3 scaffold).

---

## TASKS

### PHASE 0 — DAY-1 PROOF (gate: must pass before Phase 1)
- [x] 0.1 Init repo: `git init`, set remote to the URL above, `.gitignore`
      (node_modules, dist), first commit + push.
- [x] 0.2 Minimal MV3 extension: manifest.json (name "Friday"; permissions
      activeTab, scripting, storage; host_permissions <all_urls>), popup.html
      with a "Test AI" button + status + progress bar + output area, popup.js.
- [x] 0.3 Add Transformers.js (@huggingface/transformers), bundled to work
      offline after first load.
- [x] 0.4 On "Test AI": check `navigator.gpu`; print WebGPU available/not.
- [x] 0.5 Download + load a SMALL vision-language model (Moondream2 or SmolVLM)
      on WebGPU; WASM fallback; show download progress bar; report backend used.
- [x] 0.6 Run the model on a bundled sample screenshot with prompt "Describe this
      screen and list buttons and input fields"; print output + load time +
      inference time.
- [x] 0.7 GATE REVIEW: record the numbers in the progress log. If it runs at
      acceptable speed → proceed to Phase 1. If too slow → switch to a smaller
      model / lean harder on detectors before continuing.
      **CLOSED — VLM proven, deferred as optional fallback.** See progress log.

### PHASE 1 — EXTENSION SHELL + UI
- [x] 1.1 Background service worker + message routing (popup/content <-> worker).
- [x] 1.2 Build the side-panel UI to match the UI DESIGN SPEC above (header,
      empty-state body, input bar). Light theme, crimson accent. Save the
      reference mockup to `design/friday-ui.png`.
- [x] 1.3 Wire header controls: Chat/Agent dropdown, Cloud/On-Device toggle,
      settings gear (open a settings view stub).
- [x] 1.4 Content script skeleton injected into the active tab.
- [x] 1.5 Screen capture (chrome.tabs.captureVisibleTab) + read DOM /
      accessibility tree; return a structured snapshot.
- [x] 1.6 First-run model download with progress bar; cache + reuse (no
      re-download on later runs).

### PHASE 2 — PRIVACY LAYER (graded core)
- [x] 2.1 DOM PII detector: find password, email, name, ID/Aadhaar/PAN fields.
- [x] 2.2 Face detection with BlazeFace on the captured screen.
- [x] 2.3 OCR text-PII with Tesseract.js (emails, numbers, IDs in images).
- [x] 2.4 Redaction engine: Canvas mask/blur over every detected region, on-device.
- [x] 2.5 Privacy receipt UI: show exactly what was redacted before any send.
- [x] 2.6 Build sanitized payload (redacted image + safe DOM structure).

### PHASE 3 — CONTROL LAYER (99% accuracy)
- [x] 3.1 DOM-anchored executor: click(target), type(target,text), scroll —
      targeting real elements via the accessibility tree, not pixel guessing.
- [x] 3.2 Element resolver: map an intent ("submit button") to the exact DOM node.
- [x] 3.3 Verify tiny buttons/links/fields are hit reliably; log accuracy.

### PHASE 4 — BRAIN ROUTER + REASONING
- [x] 4.1 Router interface: (task + sanitized context) -> action, behind ONE
      config value so sources swap without a rewrite.
- [x] 4.2 Source A — local in-browser VLM (default, offline).
- [x] 4.3 Source B — BYOK: settings page to store an API key
      (chrome.storage.local, encrypted); route hard tasks to Gemini/OpenAI/Groq.
      Wire this to the header Cloud/On-Device toggle + "Select Model" dropdown.
- [x] 4.4 ReAct-style loop: plan → tool/action → observe → repeat, with a Stop.

### PHASE 5 — VOICE (Friday)
- [x] 5.1 Web Speech API STT for command input (wire to the mic button).
- [x] 5.2 Wake word "Hey Friday" (lightweight).
- [x] 5.3 TTS responses; full hands-free flow: speak → see → redact → act → speak.

### PHASE 6 — MCP (stretch / wow)
- [x] 6.1 MCP client; settings to enable MCP servers.
- [x] 6.2 Agent can call an external tool (e.g. Gmail/Notion); PII redacted first.

### FINAL
- [x] F.1 One flawless end-to-end demo task (form with face + password + email:
      capture → redact → action → execute), on a controlled test page.
- [x] F.2 First-run UX polish (progress, clear status), error handling.
- [x] F.3 Record demo video + write a short README.
      README done. Demo video not recorded — that step needs a human.

---

## DONE WHEN
- Installing the extension works with zero manual setup (model self-downloads,
  cached after).
- A voice/text command reads the screen, visibly redacts PII, and executes the
  action accurately (including small buttons).
- Runs fully on-device by default; BYOK available for harder tasks.
- Privacy receipt proves nothing sensitive left the machine.
- The side panel matches the UI DESIGN SPEC.
- Every task committed and pushed to the repo.
